# impala-query 에 필요하다.
impyla>=0.18.0

# LDAP(PLAIN)과 Kerberos(GSSAPI) 인증에 필요하다. impyla는 auth_mechanism이
# NOSASL이 아니면 접속 시점에 이 둘을 임포트한다. 인증 없는 환경이라면 빼도 된다.
#
# 데비안/우분투에서 "Failed building wheel for pure-sasl" 이 나면 PEP 517로 빌드한다:
#     pip install --use-pep517 pure-sasl thrift-sasl
pure-sasl>=0.6.2
thrift-sasl>=0.4.3

# s3-ops 에 필요하다.
boto3>=1.26.0

# 설정 파일(conf/config.yaml)을 읽는 데 필요하다.
# --no-config 로만 쓴다면 없어도 된다.
PyYAML>=6.0

# gp-query 에 필요하다.
psycopg2-binary>=2.9.5

# sql/ 의 .sql 템플릿에 변수를 채울 때 필요하다.
# {{ }} 나 {% %} 가 없는 평범한 SQL만 쓴다면 없어도 된다.
Jinja2>=3.0
